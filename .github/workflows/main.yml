name: Build ArmsFix+ (Linux)

on:
  push:
    branches:
      - main
      - master
    tags:
      - "v*"
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    container:
      image: ubuntu:20.04
    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Linux build dependencies
        run: |
          apt-get update
          DEBIAN_FRONTEND=noninteractive apt-get install -y \
            git \
            python3 \
            python3-pip \
            build-essential \
            gcc-multilib \
            g++-multilib \
            libc6-dev-i386

      - name: Install AMBuild
        run: python3 -m pip install --upgrade pip "git+https://github.com/alliedmodders/ambuild"

      - name: Clone SDK dependencies
        run: |
          git clone --depth 1 --branch csgo https://github.com/alliedmodders/hl2sdk.git hl2sdk-csgo
          git clone --depth 1 --recurse-submodules https://github.com/alliedmodders/metamod-source.git metamod-source
          git clone --depth 1 --recurse-submodules https://github.com/alliedmodders/sourcemod.git sourcemod
          git -C metamod-source submodule update --init --recursive
          git -C sourcemod submodule update --init --recursive

      - name: Configure extension
        run: |
          mkdir -p src/build
          cd src/build
          python3 ../configure.py --sdks csgo --enable-optimize

      - name: Build extension
        run: |
          cd src/build
          ambuild

      - name: Show build output
        run: |
          ls -R src/build

      - name: Assemble release package
        run: |
          rm -rf dist
          mkdir -p dist/package/addons/sourcemod/scripting/include
          cp -r src/build/package/* dist/package/
          cp addons/sourcemod/scripting/include/armsfix_plus.inc dist/package/addons/sourcemod/scripting/include/
          cp addons/sourcemod/extensions/no_ext.txt dist/package/addons/sourcemod/extensions/
          cd dist
          tar -czf armsfix-plus-linux.tar.gz package

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: armsfix-plus-linux
          path: |
            dist/package/**
            dist/armsfix-plus-linux.tar.gz
